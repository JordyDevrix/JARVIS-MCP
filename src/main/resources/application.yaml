spring:
  application:
    name: JARVIS
  ai:
    mcp:
      server:
        name: JARVIS
        version: 0.0.1
        protocol: STREAMABLE

server:
  port: ${SERVER_PORT:8080}

springdoc:
  api-docs.enabled: true
  swagger-ui:
    enabled: true
    path: /docs
